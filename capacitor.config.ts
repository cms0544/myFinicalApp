import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.myfinicalApp',
  appName: 'myFinicalApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
